	<div class = "jumbotron">
		<div class= "container">
			<% if user_signed_in? %>
				<%= link_to "Log Out", destroy_user_session_path, method: :delete %> | 
				<%= link_to "Account Settings", edit_user_registration_path %> 
				<% end %>
			<%= image_tag ('ministrylogo.png') %>
		</div>
	</div>